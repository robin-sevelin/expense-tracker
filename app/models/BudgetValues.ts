export interface IReccuringIncome {
  title: string;
  amount: number;
  date: string;
}

export interface IReccuringExpense {
  title: string;
  amount: number;
  date: string;
}

export interface IBalance {
  amount: number;
}
