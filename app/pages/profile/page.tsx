import UserPage from '@/components/profilePage/UserPage';

const Profile = () => {
  return (
    <section className='w-full'>
      <h2 className='text-5xl font-bold flex justify-center'>PROFILE</h2>
      <UserPage />
    </section>
  );
};

export default Profile;
